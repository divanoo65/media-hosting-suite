server {
    listen 1001;
    server_name _;

    # Static files served from IMAGE_DIR
    location / {
        root /var/www/images;
        autoindex on;
        add_header Access-Control-Allow-Origin *;
        add_header Cache-Control "public, max-age=3600";
    }
}
